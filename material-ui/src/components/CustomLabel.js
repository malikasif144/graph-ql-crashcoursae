import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    label: {
      fontWeight: 'bold',
      fontSize: 32,
      for:'custom label',
      color:'black'
    },
  },
});

export default theme